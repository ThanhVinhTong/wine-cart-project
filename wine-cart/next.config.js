/** @type {import('next').NextConfig} */
const nextConfig = {}

module.exports = {
    reactStrictMode: true, 
    images: {
      domains: ['console.cloud.google.com', 'lh3.googleusercontent.com'],
    },
}
